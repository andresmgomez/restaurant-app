import { colors } from "./colors";
import { fonts } from "./fonts";
import { cards } from "./cards";

const spaces = ["6px", "10px", "16px", "28px", "32px"];

export const theme = {
  spaces,
  colors,
  fonts,
  cards,
};
